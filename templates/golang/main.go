// Starter entry point so `go build`, `go vet`, and `go test` pass on a fresh
// scaffold. Replace with your application.
package main

import "fmt"

func main() {
	fmt.Println("Hello from {{PROJECT_NAME}}")
}
