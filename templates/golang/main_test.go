package main

import "testing"

// Starter test so `go test ./...` passes on a fresh scaffold. Replace with real tests.
func TestSmoke(t *testing.T) {
	if 1+1 != 2 {
		t.Fatal("arithmetic is broken")
	}
}
